version: '3.8'

services:
  server:
    build: ./server
    container_name: taskflow_server
    restart: on-failure
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://ashish9696yadav97040_db_user:ashishYadav9696%23@todolistclustor.wrxpbtw.mongodb.net/fitplusfitnessdatabase?retryWrites=true&w=majority&appName=todolistclustor
      - PORT=5000
      - NODE_ENV=production
    networks:
      - taskflow_net

  client:
    build: ./client
    container_name: taskflow_client
    restart: unless-stopped
    ports:
      - "5173:5173"
    depends_on:
      - server
    networks:
      - taskflow_net

networks:
  taskflow_net:
    driver: bridge
